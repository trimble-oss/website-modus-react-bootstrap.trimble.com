---
title: Tables
description: Tables display information in a grid-like format of rows and columns. They organize information in a way that’s easy to scan, so that users can look for patterns and insights.
---

import { graphql } from "gatsby"
import {
  TableBasic,
  TableHoverable,
  TableIconsControls,
  TableSmall,
  TableWithSorting,
  TableWithScroll,
  TableWithFixedHeader,
  TableWithPagination,
  MakeData as makeData,
  TableWithColumnResize,
  TableWithSingleRowSelection,
  TableWithMultiRowSelection,
  TableWithCheckBoxSelection,
  TableWithCustomCheckBoxSelection,
  TableWithStickyFirstColumn,
} from "../../examples/components/Table"
import Badge from "@trimbleinc/modus-react-bootstrap/Badge"

<Overview>
  Due to the widespread use of tables across third-party widgets like calendars
  and date pickers, we’ve designed our tables to be opt-in. Use{" "}
  <code>&lt;Table&gt;</code>, and extend with custom styles or our various
  included modifier classes.
</Overview>

<CodeBlock title="Basic Table" code={TableBasic}></CodeBlock>

<CodeBlock title="Hoverable Rows" code={TableHoverable}>
  Add prop <code>hover </code> to enable a hover state on table rows within
  <code>&lt;tbody&gt;</code>.
</CodeBlock>

<CodeBlock
  title="Tables with Icons and Controls"
  code={TableIconsControls}
></CodeBlock>

<CodeBlock title="Small Variants" code={TableSmall}>
  <p>
    All previous table styles are also available in a smaller variant, set prop{" "}
    <code>size</code> to <code>sm</code>
  </p>
  <p>
    When using small variants of tables with icons and controls, don’t forget to
    use the smaller variants of those icons and controls too!
  </p>
</CodeBlock>

<LinkedHeading h="2" className="h1" id="datatable">
  <span>
    DataTable
    <Badge pill variant="tertiary" className="mr-2 align-top">
      BETA
    </Badge>
  </span>
</LinkedHeading>
<p>
  To enable advanced features like sorting, use the <code> DataTable </code>{" "}
  component along with the basic <code> Table </code> and additional components
  such as
  <code> TableHead, TableRow, TableBody and TableCell</code>.
  <br />
  <br />
  <p>
    The <code>DataTable</code> component requires two props:
    <code> columns </code> which takes an array of header data and
    <code> data </code> prop which takes an array of row data. The row data
    should match the header information passed to columns prop.
    <br />
    The objects passed in the array to the <code> column </code> prop has a
    required key <code> accessor </code> which is the column identifier and and
    the <code> Header </code> key is used to set the column label, eg.
    {`[{ Header: 'First Name', accessor: 'firstName' }]`}.
  </p>
</p>

<CodeBlock title="Table with Sorting" code={TableWithSorting} noInline={true}>
  <p>
    Use the prop <code> hasSorting </code> to enable/disable the sorting on the
    entire <code> Table </code> and prop
    <code> sortBy </code> can be used on the column object to control the sorting
    at a column level.
  </p>
  <p>
    A column can be sorted by clicking on the header. Repeat this action to
    change the sorting order.
  </p>
</CodeBlock>

<CodeBlock
  title="Table with Scroll"
  scope={{
    makeData,
  }}
  code={TableWithScroll}
  noInline={true}
>
  Setting height & width on <code> TableContainer </code> will enable vertical &
  horizontal scrolling.
</CodeBlock>

<CodeBlock
  title="Table with Sticky Header"
  scope={{
    makeData,
  }}
  code={TableWithFixedHeader}
  noInline={true}
>
  Use the <code> sticky-top </code> class to ensure that the table's header is
  always visible while scrolling.
</CodeBlock>

<CodeBlock
  title="Table with Client-side Pagination"
  scope={{
    makeData,
  }}
  code={TableWithPagination}
  noInline={true}
>
  Use the <code> TablePagination </code> component along with{" "}
  <code> Table </code> to display the table rows in multiple pages and set the
  prop <code> hasPagination </code> on <code> DataTable </code> to true.
</CodeBlock>

<CodeBlock
  title="Table with Columns Resizing"
  scope={{
    makeData,
  }}
  code={TableWithColumnResize}
  noInline={true}
>
  Set the prop <code> resizeColumns </code> to true on <code> DataTable </code>
  to enable resizing on all the columns. For more control use
  <code> disableResizing </code> on the columns object as shown in the example.
</CodeBlock>

<CodeBlock
  title="Table with Single Row Selection"
  scope={{
    makeData,
  }}
  code={TableWithSingleRowSelection}
  noInline={true}
>
  To be able to select a row by clicking anywhere inside it, invoke the function{" "}
  <code>row.toggleRowSelected()</code> to toggle the row's selected state.
</CodeBlock>

<CodeBlock
  title="Table with Multiple Row Selection"
  scope={{
    makeData,
  }}
  code={TableWithMultiRowSelection}
  noInline={true}
>
  Single row selection is the default so to enable multi-row selection use the
  prop <code> multipleRowSelection</code>. Clicking on a row selects it and
  clicking again undo the selection.
</CodeBlock>

<CodeBlock
  title="Table with CheckBox Row Selection"
  scope={{
    makeData,
  }}
  code={TableWithCheckBoxSelection}
  noInline={true}
>
  Use prop <code> checkBoxRowSelection </code> on the
  <code> DataTable </code> component for displaying checkbox to control row selection
  and add a <code> TableHeaderCell </code> with <code>accessor='selector'</code>.
</CodeBlock>

<CodeBlock
  title="Table with Custom CheckBox Row Selection"
  scope={{
    makeData,
  }}
  code={TableWithCustomCheckBoxSelection}
  noInline={true}
>
  Row Selection Checkbox can be configured by using <code> Header </code> and
  <code> Cell </code> functions in the column object that is passed to
  <code> DataTable</code>. For single row selection, <code> Header </code>
  function should not be used.
</CodeBlock>

<CodeBlock
  title="Table with Sticky First Column"
  scope={{
    makeData,
  }}
  code={TableWithStickyFirstColumn}
  noInline={true}
>
  Use the custom class <code> table-sticky-first-column </code> on{" "}
  <code> Table </code> component to make first column fixed.
</CodeBlock>

<CodeBlock
  title="Hiding Columns"
  scope={{
    makeData,
  }}
  code={TableWithPagination}
  noInline={true}
>
  Right clicking on a column header gives a context menu option to collapse/show
  columns. When a column is collapsed, a visual indicator with arrows is
  displayed and clicking on it will show the column.
</CodeBlock>

<LinkedHeading h="2" className="h1" id="table-api">
  API
</LinkedHeading>
<ComponentApi metadata={props.data.Table} />
<ComponentApi metadata={props.data.DataTable} />
<ComponentApi metadata={props.data.TableHead} />
<ComponentApi metadata={props.data.TableHeaderCell} />
<ComponentApi metadata={props.data.TableBody} />
<ComponentApi metadata={props.data.TableRow} />
<ComponentApi metadata={props.data.TableCell} />
<ComponentApi metadata={props.data.TableContainer} />
<ComponentApi metadata={props.data.TablePagination} />

export const query = graphql`
  query TableQuery {
    Table: componentMetadata(displayName: { eq: "Table" }) {
      ...ComponentApi_metadata
    }
    DataTable: componentMetadata(displayName: { eq: "DataTable" }) {
      ...ComponentApi_metadata
    }
    TableContainer: componentMetadata(displayName: { eq: "TableContainer" }) {
      ...ComponentApi_metadata
    }
    TableHead: componentMetadata(displayName: { eq: "TableHead" }) {
      ...ComponentApi_metadata
    }
    TableHeaderCell: componentMetadata(displayName: { eq: "TableHeaderCell" }) {
      ...ComponentApi_metadata
    }
    TableBody: componentMetadata(displayName: { eq: "TableBody" }) {
      ...ComponentApi_metadata
    }
    TableRow: componentMetadata(displayName: { eq: "TableRow" }) {
      ...ComponentApi_metadata
    }
    TableCell: componentMetadata(displayName: { eq: "TableCell" }) {
      ...ComponentApi_metadata
    }
    TablePagination: componentMetadata(displayName: { eq: "TablePagination" }) {
      ...ComponentApi_metadata
    }
  }
`
